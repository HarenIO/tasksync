name: Exploring contexts
on:
    push:
        branches-ignore:
            - main
jobs:
    print-branch-name:
        runs-on: ubuntu-latest
        steps:
            - name: Print branch name
              run: "echo 'Branch name: ${{ env.GITHUB_REF_NAME }}'"
